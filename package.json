{
  "name": "voice-keyboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "runtime:download": "powershell -ExecutionPolicy Bypass -File scripts\\download-llama-runtime.ps1",
    "tauri": "tauri",
    "pretauri:dev": "npm run runtime:download",
    "tauri:dev": "tauri dev",
    "pretauri:build": "npm run runtime:download",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}

